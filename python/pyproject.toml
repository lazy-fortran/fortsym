[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "fortsym"
version = "0.1.0"
description = "Native Python bindings for the fortsym C ABI"
readme = "README.md"
requires-python = ">=3.9"

[tool.setuptools]
packages = ["fortsym"]
